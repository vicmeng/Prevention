<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.bcu.ls.mapper.UserMapper">



	<insert id="insertUser"
		parameterType="cn.eud.bcu.ls.entity.User">
		insert into user
		(user_name,user_pwd,user_school,user_integral,user_phonenumber,user_type,user_schoolnumber,class_id,dormitory_id)
		values
		(#{user_name},#{user_pwd},#{user_school},#{user_integral},#{user_phonenumber},#{user_type},#{user_schoolnumber},#{class_id},#{dormitory_id})
	</insert>

	<delete id="deleteUser" parameterType="int">
		delete from user where
		user_id =#{u_id}
	</delete>

	<select id="queryUsers" resultType="cn.eud.bcu.ls.entity.User">
		select * from user
	</select>
	
	<select id="queryUserById" parameterType="int" resultType="cn.eud.bcu.ls.entity.User">
		select * from user where user_id=#{u_id}
	</select>
	
	<update id="updataUser" parameterType="cn.eud.bcu.ls.entity.User">
		update user set user_name=#{user_name},user_pwd=#{user_pwd},user_school=#{user_school},user_integral=#{user_integral},user_phonenumber=#{user_phonenumber},user_type=#{user_type},user_schoolnumber=#{user_schoolnumber} where user_id=#{user_id}
	</update>
	
</mapper>